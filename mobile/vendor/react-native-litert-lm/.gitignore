# Dependencies
node_modules/

# Build outputs
lib/
build/
*.tsbuildinfo

# Native builds
android/build/
android/.gradle/
android/.cxx/
android/app/build/
ios/build/
ios/Pods/
*.xcworkspace

# Nitrogen generated (can be regenerated)
nitrogen/generated/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Expo
.expo/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Model files (large, should not be committed)
*.litertlm

# Scratch downloads (HF bundles, large) and eval outputs
scratch/
eval/out/

# iOS frameworks (built from source or downloaded from releases)
# Prebuilt framework zip (uploaded to GitHub Releases, not committed)
# Bazel build cache for LiteRT-LM source compilation
.litert-lm-build/
# Probe (issue #1): clone, built binaries, symlinks, fixtures, build output —
# only the SwiftPM scaffold + scripts are committed (see probe/.gitignore)
probe/.PhoneClaw/
probe/.cache/
probe/.pkg-work/
probe/.probe*
probe/PhoneClawProbe/Frameworks/
probe/PhoneClawProbe/Sources/PhoneClawEngine
probe/PhoneClawProbe/.build/
probe/PhoneClawProbe/.swiftpm/
probe/PhoneClawProbe/Tests/PhoneClawProbeTests/Resources/
*.pte
*.bin
*.gguf
*.onnx
*.tflite
*.mlmodel
*.mlpackage

# Documentation
docs/